# === 智简灵析 后端环境变量示例 ===
# 复制为 .env 并自行加载（或用 shell/系统环境变量），或直接改 backend/config.py 中的默认值。
#
# 注意：后端未使用 python-dotenv，.env 文件不会被自动读取。
#       生效方式二选一：
#       1) 在运行前于 shell 中 export（或 Windows PowerShell 中 $env:VAR="..."）
#       2) 直接编辑 backend/config.py 里的默认值

# LLM 服务商 base_url（OpenAI 兼容协议）
# 默认：https://api.siliconflow.cn/v1 （硅基流动）—— 未设置时走 config.py 默认
#LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4    # 智谱 AI
#LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1   # 通义

# LLM API Key（强烈建议在应用「设置」页填写，会存 SQLite settings 表，优先于此处）
#LLM_API_KEY=sk-xxxxxxxx

# 浏览器调试端口（接管模式默认 9222，改写在 config.py BROWSER_CDP_PORT）